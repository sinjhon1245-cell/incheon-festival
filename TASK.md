# TASK.md

## 2026년 인천 AI·SW미래채움 교육페스티벌 관계자 운영 포털 전면 개편

현재 프로젝트를 **「2026년 인천 AI·SW미래채움 교육페스티벌」의 일반 관람객용 홍보 사이트가 아니라, 행사 관계자가 준비 단계와 행사 당일 실제로 사용하는 운영 포털**로 전면 개편한다.

현재 공개 사이트:

- https://incheon-aisw-festival.netlify.app/

기능 및 운영 UX 참고 사이트:

- https://incheonplay.kr/

이번 작업에서는 기존 페이지 구조를 보존할 필요가 없다.  
필요하면 HTML 구조, 섹션 구성, JavaScript 구조, Supabase 스키마, 관리자 페이지 구조까지 크게 바꿔도 된다.

다만 현재 사이트가 가지고 있는 전반적인 디자인 인상은 유지한다.

- 흰색 중심 배경
- 짙은 남색 본문
- 보라색 포인트
- 깔끔하고 현대적인 교육행사 분위기
- 과도한 장식보다 정보 위계가 명확한 카드 UI
- Pretendard 계열의 읽기 쉬운 한글 UI
- 기존 디자인에 있는 좋은 요소는 재사용
- 화려한 3D, 과한 그라데이션, 불필요한 애니메이션은 추가하지 말 것

이번 개편의 최우선 목표는 **디자인이 아니라 기능과 실제 행사 운영 편의성**이다.

---

# 0. 작업 시작 전 반드시 먼저 할 일

코드를 바로 수정하지 말고 프로젝트 전체를 먼저 분석한다.

다음을 확인한다.

1. 현재 파일/폴더 구조
2. 현재 사용 중인 기술 스택
3. 공개 사이트 렌더링 방식
4. 관리자 페이지 구조
5. Supabase 연결 구조
6. `assets/config.js` 또는 동등한 설정 파일
7. 기존 Supabase schema 또는 migration
8. 현재 사용 중인 테이블
9. fallback-data 구조
10. Netlify 배포 설정
11. git 현재 상태
12. 현재 branch
13. git remote
14. 아직 commit되지 않은 사용자 작업이 있는지

반드시 다음을 확인한다.

```bash
git status
git branch --show-current
git remote -v
```

기존 사용자의 변경사항이 있다면 절대 임의로 삭제하거나 reset하지 않는다.

현재 구현을 충분히 분석한 뒤 기존 코드 중 재사용할 수 있는 부분과 폐기할 부분을 판단한다.

React, Next.js 등 새로운 프레임워크로 마이그레이션하는 것은 특별한 이유가 없는 한 하지 않는다.  
현재 정적 HTML/CSS/JS + Supabase 구조로 충분하다면 그 구조를 유지하면서 재설계한다.

작업 전에 현재 공개 사이트와 참고 사이트의 실제 화면 및 기능 구조도 확인하고, 단순 디자인 모방이 아니라 **관계자용 운영 UX**에 필요한 요소를 선별해서 반영한다.

---

# 1. 사이트의 정체성을 완전히 변경

기존:

> 행사를 구경하러 오는 일반 방문객을 위한 페스티벌 소개 사이트

에서 다음으로 변경한다.

> 행사 관계자가 준비 단계와 행사 당일 스마트폰으로 실제 사용하는 모바일 중심 운영 포털

따라서 기존 방문객 중심 콘텐츠는 핵심 메뉴에서 제거하거나 관계자 관점으로 재작성한다.

예:

- 누구나 참여 가능
- 참가비 무료
- 일반 관람객 참여 안내
- 일반 방문객 FAQ
- 홍보 중심 프로그램 소개
- 지나치게 큰 Hero 영역
- 긴 행사 소개 문단
- 단순 홍보용 숫자 카드

기존 프로그램, 시간표, 부스, 주차, 장소 관련 데이터는 버리지 말고 **운영 데이터로 재구성하여 재사용**한다.

사이트에 접속한 관계자가 가장 먼저 알고 싶은 것은 아래다.

1. 지금 무엇을 해야 하는가
2. 현재 무엇이 진행 중인가
3. 다음 일정은 무엇인가
4. 어떤 부스에 문제가 있는가
5. 긴급 공지가 있는가
6. 현장 문제를 어디에 보고해야 하는가
7. 누구에게 연락해야 하는가
8. 운영자료를 어디서 찾는가

이 흐름을 최우선으로 설계한다.

---

# 2. 관계자 로그인

관계자용 사이트이므로 로그인하지 않은 사용자에게 내부 운영정보가 그대로 노출되면 안 된다.

Supabase Authentication의 이메일/비밀번호 로그인을 사용한다.

회원가입 기능은 만들지 않는다.

로그인하지 않았을 때는 간결한 로그인 화면만 보여준다.

예시:

```text
2026년 인천 AI·SW미래채움 교육페스티벌

행사 운영 포털

2026. 10. 17.(토)

[이메일]
[비밀번호]

[관계자 로그인]
```

행사 장소 등 실제로 확정되지 않은 정보는 임의로 새로 만들어 넣지 않는다.

로그인 실패 메시지는 이해하기 쉽게 표시한다.

비밀번호, secret key, service_role key는 프론트 코드에 절대 넣지 않는다.

Supabase에서는 Publishable/anon key만 사용한다.

로그인 상태를 유지할 수 있게 하고, 새로고침 후에도 세션이 정상 복구되어야 한다.

로그아웃 기능도 제공한다.

---

# 3. 로그인 후 첫 화면 — 운영 대시보드

이 페이지가 사이트에서 가장 중요하다.

기존처럼 큰 Hero를 만들지 말고 **첫 화면에서 현재 행사 운영 상황이 바로 보이게 한다.**

상단에는 다음을 표시한다.

- 2026년 인천 AI·SW미래채움 교육페스티벌
- 행사일
- 운영시간
- D-day
- 현재 날짜와 시간

행사 전:

```text
D-38
```

행사 당일:

```text
행사 진행 중
현재 10:42
```

행사 종료 후:

```text
행사 종료
```

와 같이 상태가 자동으로 바뀌게 한다.

---

## 3-1. 핵심 운영 현황

홍보 숫자가 아니라 실제 운영 숫자를 보여준다.

예:

- 전체 부스
- 운영 중
- 준비 중
- 일시 중단
- 오늘 프로그램
- 현재 진행 중 프로그램
- 긴급 공지
- 미처리 운영 요청

가능하면 실제 DB 데이터를 집계해서 자동 계산한다.

숫자를 HTML에 하드코딩하지 않는다.

각 상태 카드를 누르면 관련 목록으로 바로 이동하도록 한다.

예:

- `운영 중 36` → 부스 현황에서 운영 중 필터
- `미처리 요청 3` → 운영 요청에서 미완료 필터
- `긴급 공지 1` → 긴급공지 목록

---

# 4. 「지금 / 다음 일정」

관계자가 행사장에서 가장 자주 확인하는 기능이다.

현재 시각을 기준으로 자동 계산한다.

예:

```text
지금 진행 중

10:30–11:10
AI교육 특별강연
대강당
담당: 미래교육팀
```

그 아래:

```text
다음 일정

11:20
학생 AI 프로젝트 발표
야외무대

38분 후
```

현재 시간이 일정 사이의 공백인 경우에는 억지로 "진행 중"을 만들지 말고:

```text
현재 진행 중인 일정이 없습니다.
다음 일정까지 18분 남았습니다.
```

처럼 표시한다.

전체 운영 일정도 시간순으로 연결해서 보여준다.

- 현재 진행 중인 일정은 강조
- 종료된 일정은 시각적으로 약하게 표시
- 예정 일정은 정상 표시

필터:

- 전체
- 무대
- 강연
- 부스
- 운영
- 행사 지원

검색 기능도 가능하면 제공한다.

---

# 5. 전체 일정

기존 시간표 기능을 관계자용 운영 일정으로 업그레이드한다.

각 일정 데이터는 최소한 다음 필드를 지원한다.

- 제목
- 시작시간
- 종료시간
- 장소
- 분류
- 담당팀
- 담당자
- 운영 메모
- 상태
- 정렬순서

모바일에서는 타임라인 방식이 기본이다.

PC에서는 필요하다면 타임라인/표 보기 전환을 지원해도 된다.

현재 시각 자동 강조 기능을 반드시 구현한다.

일정 상태는 다음과 같이 자동 또는 수동으로 구분할 수 있다.

- 예정
- 진행 중
- 종료
- 취소
- 변경

취소 또는 변경된 일정은 관계자가 즉시 식별할 수 있어야 한다.

---

# 6. 부스 운영 현황

이 기능은 핵심 기능이다.

기존의 단순 부스 소개 목록을 **운영현황 시스템**으로 변경한다.

상단 요약:

- 전체
- 준비 전
- 준비 완료
- 운영 중
- 일시 중단
- 운영 종료

검색:

```text
부스번호 / 학교 / 기관 / 프로그램명 검색
```

상태 필터:

- 전체
- 준비 전
- 준비 완료
- 운영 중
- 일시 중단
- 운영 종료

구역 필터:

- 전체
- A존
- B존
- C존
- D존
- 기타

실제 행사 구역 명칭이 확정되어 있지 않다면 관리자에서 수정할 수 있게 하고, 임의로 확정 정보처럼 표시하지 않는다.

각 부스 카드에는 최소한 다음을 표시한다.

```text
A-01

생성형 AI로 만드는 나만의 동화

인천○○초등학교

● 운영 중

담당자 홍길동
전기 사용
네트워크 필요

[상세 보기]
```

모바일에서는 작은 화면에서도 상태가 한눈에 들어오게 한다.

---

# 7. 부스 상세

부스를 선택하면 상세 페이지, drawer 또는 modal로 상세 정보를 보여준다.

포함 정보:

- 부스 번호
- 부스명
- 운영기관
- 구역
- 담당자
- 담당자 연락처
- 운영 프로그램
- 운영 시간
- 전기 사용 여부
- 네트워크 필요 여부
- 테이블/의자 등 필요 물품
- 운영 메모
- 특이사항
- 현재 운영 상태
- 최근 변경 시각

상태:

- 준비 전
- 준비 완료
- 운영 중
- 일시 중단
- 운영 종료

관리 권한 또는 허용된 staff 권한이 있는 경우 여기에서 상태를 변경할 수 있게 한다.

상태 변경은 실제 Supabase DB에 저장되어야 한다.

새로고침 후에도 유지되어야 한다.

휴대전화에서는 전화번호를 누르면 바로 전화할 수 있도록 `tel:` 링크를 지원한다.

---

# 8. 긴급공지 / 운영공지

일반 게시판이 아니라 **현장 공지 시스템**으로 만든다.

공지 등급:

- 긴급
- 중요
- 일반

예:

```text
긴급

B구역 전력 점검 중입니다.
B12~B17 부스는 전원을 잠시 사용하지 마세요.

10:42 업데이트
```

기능:

- 공지 목록
- 최신순 정렬
- 고정 공지
- 긴급공지 강조
- 작성 시각
- 수정 시각
- 작성자 또는 담당부서
- 읽기 쉬운 상세 화면

대시보드 최상단에는 긴급 공지가 존재할 경우 자동 표시한다.

관리자는 공지:

- 작성
- 수정
- 삭제
- 고정
- 긴급/중요/일반 변경

을 할 수 있어야 한다.

Supabase Realtime 적용이 어렵지 않다면 공지와 부스 상태는 새로고침 없이 갱신되도록 구현한다.

다만 안정성을 해치면서까지 복잡하게 만들지는 않는다.

---

# 9. 운영 요청 / 현장 문제 보고

이번 사이트의 실무 활용도를 높이는 핵심 기능이다.

관계자가 현장에서 간단하게 문제를 등록할 수 있게 한다.

대시보드 또는 모바일 화면에 접근성이 높은 버튼을 둔다.

```text
+ 현장 문제 보고
```

입력 항목:

- 부스 또는 위치
- 유형
- 우선순위
- 제목
- 내용
- 등록자
- 등록시각

유형:

- 전기
- 네트워크
- 기자재
- 시설
- 안전
- 물품
- 주차
- 기타

우선순위:

- 긴급
- 높음
- 보통

처리 상태:

- 접수
- 확인 중
- 처리 중
- 완료

예:

```text
긴급

A-17
멀티탭 전원이 들어오지 않음

10:43 접수
시설지원팀
처리 중
```

대시보드에서는 미처리 요청 건수를 자동으로 보여준다.

관리자는 요청별 상태를 변경할 수 있어야 한다.

가능하면 담당팀 또는 담당자를 지정할 수 있게 한다.

모바일에서 빠르게 등록할 수 있어야 한다.

사진 업로드 기능은 구현 부담이 과도하면 이번 버전에서는 제외해도 된다.

우선 텍스트 기반 운영 요청 시스템을 안정적으로 완성한다.

---

# 10. 운영 연락망

행사 당일 관계자가 자주 확인할 수 있도록 한다.

카테고리 예:

- 운영 총괄
- 행사 진행
- 부스 운영
- 시설
- 전기
- 네트워크
- 안전
- 보건
- 주차
- 기타

각 연락처 데이터:

- 이름
- 소속
- 담당업무
- 전화번호
- 메모
- 정렬순서

기능:

- 검색
- 카테고리 필터
- 모바일 전화 버튼
- 담당업무 기준 빠른 탐색

전화번호는 로그인 사용자에게만 노출한다.

휴대전화에서는 `tel:` 링크를 제공한다.

---

# 11. 행사장 / 공간 안내

기존 일반 관람객 오시는 길보다 관계자에게 필요한 정보 중심으로 변경한다.

표시 대상 예:

- 운영본부
- 부스 구역
- 메인무대
- 강연장
- 물품보관 장소
- 보건/구급
- 화장실
- 휴게공간
- 전기 담당 위치
- 네트워크 담당 위치
- 주차
- 운영차량 하차 위치
- 비상동선

현재 실제 배치도 이미지가 없다면 가짜 지도를 새로 만들어 사실처럼 표시하지 않는다.

대신:

- "배치도 준비 중" 상태
- 관리자에서 이미지 URL 또는 파일 경로 등록
- 향후 실제 배치도 교체 가능

구조로 만든다.

기존 사이트의 예시 주소, 예시 지도, 예시 주차 정보가 실제 확정 정보처럼 보이지 않도록 정리한다.

---

# 12. 관계자 자료실

행사 관계자가 자주 찾는 문서를 한곳에서 열 수 있게 한다.

예:

- 행사 운영계획
- 전체 일정표
- 부스 배치도
- 부스 운영자 안내
- 안전교육 자료
- 주차 안내
- 비상연락망
- 운영 매뉴얼
- 기타 공지자료

필드:

- 제목
- 분류
- 설명
- URL
- 정렬순서
- 공개 여부
- 등록일

파일 자체를 Supabase Storage에 업로드하는 기능까지 만들 필요가 없다면 우선 URL 방식으로 구현해도 된다.

새 탭으로 열기 또는 다운로드 가능하게 한다.

---

# 13. 관계자 FAQ / 운영 매뉴얼

기존 일반 방문객 FAQ는 관계자 FAQ로 변경한다.

예:

- 행사장에는 몇 시까지 도착해야 하나요?
- 부스 차량은 어디에서 하차하나요?
- 전기가 안 들어오면 어디로 연락하나요?
- 와이파이는 어떻게 사용하나요?
- 부스 물품이 부족하면 어떻게 하나요?
- 응급상황 발생 시 누구에게 연락하나요?
- 운영 종료 후 정리는 어떻게 하나요?

아코디언 UI를 활용한다.

관리자에서:

- 질문
- 답변
- 분류
- 정렬
- 공개 여부

를 관리할 수 있게 한다.

---

# 14. 내비게이션

PC에서는 운영툴 느낌이 나도록 다음 구조를 기본으로 한다.

- 대시보드
- 일정
- 부스 현황
- 공지
- 운영 요청
- 자료실
- 연락망
- 행사장

관리자 권한이면 추가:

- 관리자

단순 상단 메뉴보다 sidebar 또는 compact navigation을 검토해도 좋다.

다만 기존 사이트와 지나치게 동떨어진 기업용 SaaS 관리자 화면처럼 만들지는 않는다.

기존 사이트의 디자인 DNA는 유지한다.

---

# 15. 모바일 UX 최우선

행사 당일 관계자들은 대부분 스마트폰으로 사용할 가능성이 높다.

모바일에서는 하단 고정 내비게이션을 사용한다.

기본:

- 홈
- 일정
- 부스
- 공지
- 더보기

`더보기` 안에는:

- 운영 요청
- 자료실
- 연락망
- 행사장
- 로그아웃

을 배치한다.

요구사항:

- 최소 44px 터치 영역
- 지나치게 작은 글씨 금지
- 가로 스크롤 금지
- 긴 텍스트 자연스럽게 줄바꿈
- sticky header/footer가 콘텐츠를 가리지 않게 처리
- modal/drawer가 모바일에서 사용하기 쉬워야 함
- 폼 입력 시 키보드가 버튼을 가리지 않도록 고려
- 모바일에서 한 손으로 주요 기능 접근 가능

320px부터 1920px까지 정상적으로 보여야 한다.

---

# 16. 디자인 원칙

현재 사이트의 디자인 DNA는 유지하되 **운영 시스템에 맞게 정보 밀도를 높인다.**

유지:

- 흰 배경
- navy 계열 텍스트
- 보라색 accent
- 깔끔한 선
- 적절한 rounded card
- 충분한 여백
- 전문적이고 현대적인 교육행사 인상

변경:

- Hero 크기 대폭 축소
- 장식보다 정보 우선
- 핵심 정보는 첫 화면에서 확인
- 긴 설명 문단 최소화
- 상태는 chip/badge 형태로 명확하게
- 의미 없는 아이콘 남용 금지
- emoji 남용 금지
- 작은 회색 글씨 남용 금지
- 모바일에서 읽기 쉬운 14~16px 중심 UI

상태 색상은 의미가 명확해야 한다.

예:

- 정상 / 완료
- 주의 / 대기
- 긴급 / 중단

단, 색상만으로 상태를 표현하지 말고 항상 텍스트도 함께 표시한다.

---

# 17. Supabase 구조

현재 Supabase schema를 먼저 분석하고 최대한 재사용한다.

기존 테이블을 아무 생각 없이 삭제하거나 새로 만들지 않는다.

필요하면 migration SQL을 작성한다.

최소한 아래 개념을 지원하도록 한다.

- `staff_profiles`
- `schedule_items`
- `booths`
- `notices`
- `operation_requests`
- `resources`
- `contacts`

FAQ를 별도 관리한다면:

- `faqs`

행사 기본 설정이 필요하면:

- `event_settings`

현재 기존 테이블 구조가 비슷한 역할을 한다면 새로운 테이블을 중복해서 만들지 말고 확장한다.

각 주요 데이터에는 가능하면 다음을 포함한다.

- `id`
- `created_at`
- `updated_at`
- `sort_order`

로그인 사용자 정보에는 role을 둔다.

예:

- `admin`
- `staff`

권한:

## admin

- 전체 데이터 조회
- 전체 CRUD
- 공지 관리
- 일정 관리
- 부스 관리
- 운영 요청 상태 관리
- 연락망 관리
- 자료실 관리
- FAQ 관리

## staff

- 관계자 데이터 열람
- 운영 요청 작성
- 본인이 허용된 범위의 부스 상태 변경
- 공지 확인
- 연락망 확인
- 자료실 확인

RLS 정책을 반드시 검토한다.

Supabase의 secret key 또는 service_role key는 프론트엔드에 절대 넣지 않는다.

---

# 18. 관리자 페이지 전면 개편

기존 `/admin`도 그대로 두지 말고 **운영 콘텐츠 관리 CMS**로 개선한다.

관리 메뉴:

- 대시보드
- 행사 기본정보
- 일정 관리
- 부스 관리
- 공지 관리
- 운영 요청 관리
- 자료실 관리
- 연락망 관리
- FAQ 관리
- 계정/권한 관리가 현재 구조에서 안전하게 가능하다면 추가

각 관리 메뉴에서 실제로 다음 기능이 정상 작동해야 한다.

- 조회
- 추가
- 수정
- 삭제
- 정렬

운영 요청에서는:

- 접수
- 확인 중
- 처리 중
- 완료

상태 변경이 가능해야 한다.

삭제 등의 파괴적 작업에는 확인 절차를 둔다.

관리자 UI 역시 작은 글씨와 과도하게 빽빽한 레이아웃을 피한다.

---

# 19. 현재 Supabase 연결 문제 반드시 확인

현재 배포된 관리자 페이지에서는 Supabase 미연결 안내가 나타났던 상태다.

다음 가능성을 정확히 확인한다.

- 로컬에서만 연결된 상태인지
- Netlify 배포본에서 config가 누락되는지
- `assets/config.js`가 gitignore 처리되었는지
- Netlify 배포 설정 문제인지
- 잘못된 경로에서 config를 불러오는지
- config 로딩 순서 문제인지
- 환경변수를 사용해야 하는 구조인지

기존 Supabase Project URL / Publishable key 설정이 프로젝트 안에 이미 있다면 재사용한다.

secret/service_role은 절대 사용하지 않는다.

Publishable key를 사용하는 구조는 정상적이지만 RLS가 제대로 설정되어 있어야 한다.

수정 후 로컬뿐 아니라 **Netlify 배포 환경에서도 Supabase가 정상 연결되는 구조인지 확인한다.**

---

# 20. fallback-data 정책 변경

기존 공개 사이트는 Supabase 미연결 시 fallback data를 사용할 수 있지만, 관계자 사이트에서는 인증 없이 내부 운영정보가 fallback으로 공개되면 안 된다.

따라서:

- 로그인 및 내부 운영정보는 실제 Supabase 연결 기준으로 한다.
- Supabase 연결이 없으면 명확한 오류 안내를 표시한다.
- 인증을 우회해 내부 데이터가 보이는 demo/fallback 로직을 만들지 않는다.
- 기존 fallback data가 공개 페이지에서만 필요한 경우 별도 분리한다.
- 운영정보는 인증되지 않은 상태에서 절대 노출하지 않는다.

---

# 21. UI만 있는 가짜 기능 금지

모든 핵심 기능은 실제로 동작해야 한다.

다음과 같은 상태로 끝내지 않는다.

- 버튼만 있고 저장되지 않음
- 상태를 바꿨는데 새로고침하면 원상복구
- 공지 추가 버튼은 있지만 DB 저장 안 됨
- 일부 필터가 동작하지 않음
- 모바일 메뉴 링크 오류
- 관리자 CRUD가 화면만 있음
- 샘플 데이터만 바뀌고 실제 Supabase에는 저장되지 않음
- 로그인 화면만 있고 실제 인증 안 됨
- 운영 요청을 작성해도 관리자에 나타나지 않음

Supabase를 사용하는 기능은 실제 DB에 저장되고 다시 조회되어야 한다.

---

# 22. 사용성

각 화면에 다음 상태를 모두 처리한다.

- loading
- empty
- error
- success feedback

예:

```text
데이터를 불러오는 중입니다.
```

```text
등록된 공지가 없습니다.
```

```text
데이터를 불러오지 못했습니다. 다시 시도해 주세요.
```

삭제 등의 파괴적 작업에는 확인 절차를 둔다.

폼 저장 중에는 중복 클릭을 방지한다.

필수 입력값을 검증한다.

사용자에게 기술적인 Supabase error object를 그대로 보여주지 않는다.

저장 성공, 상태 변경 성공 등 필요한 경우 간결한 피드백을 제공한다.

---

# 23. 접근성과 품질

반드시 확인한다.

- semantic HTML
- label 연결
- keyboard navigation
- focus 표시
- 필요한 aria
- 충분한 contrast
- 최소 터치 영역
- 모바일 overflow
- modal/drawer focus 처리
- ESC 닫기
- loading/empty/error 상태
- 콘솔 error/warning
- 이미지 alt
- 버튼과 링크의 역할 구분

색상만으로 정보를 전달하지 않는다.

---

# 24. 기존 예시 데이터 처리

현재 사이트에 들어 있는 행사 일정, 주소, 주차, 부스 수 등은 일부가 검토용 예시 데이터일 수 있다.

없는 실제 정보를 새로 만들어 사실처럼 확대하지 않는다.

기존 데이터는 기능 테스트용으로 유지할 수 있지만, 실제 확정 정보가 아니라면:

- 관리자에서 쉽게 수정 가능하게 만들거나
- 필요한 경우 "예시 데이터"임을 명확히 표시한다.

없는 실제 행사정보를 임의로 만들어내지 않는다.

특히:

- 행사 장소
- 참여기관 수
- 부스 수
- 담당자
- 연락처
- 주차 가능 대수
- 프로그램 상세 일정

등은 확인되지 않은 값을 사실처럼 확정하지 않는다.

---

# 25. 최종 사용자 흐름

관계자가 로그인한 뒤 다음 흐름이 자연스러워야 한다.

```text
로그인
↓
운영 대시보드
↓
지금 진행 중 / 다음 일정
↓
긴급공지 확인
↓
부스 운영현황 확인
↓
필요하면 부스 상세 확인
↓
문제 발생 시 운영 요청 등록
↓
담당자 연락처 확인
↓
행사장 위치 / 운영자료 확인
```

이 흐름이 모바일에서 2~3번의 터치 안에 가능하도록 설계한다.

---

# 26. 구현 후 반드시 직접 테스트

코드 수정만 하고 끝내지 않는다.

로컬 서버를 실행하고 브라우저에서 직접 확인한다.

다음 viewport를 확인한다.

- 320px
- 375px
- 768px
- 1024px
- 1440px
- 1920px

다음 기능을 실제로 테스트한다.

1. 로그인
2. 로그아웃
3. 인증 없이 내부 페이지 접근 차단
4. 로그인 세션 유지
5. 일정 조회
6. 일정 필터
7. 현재 일정 자동 강조
8. 부스 검색
9. 부스 필터
10. 부스 상세
11. 부스 상태 변경
12. 공지 조회
13. 공지 CRUD
14. 긴급공지 대시보드 노출
15. 운영 요청 등록
16. 운영 요청 상태 변경
17. 자료실 조회
18. 연락망 검색
19. 전화 링크
20. 관리자 CRUD
21. 모바일 하단 메뉴
22. 브라우저 새로고침
23. 직접 URL 접근
24. Supabase 연결
25. 콘솔 오류
26. 접근성 기본 점검
27. 320px 모바일 overflow
28. 1920px 데스크톱 레이아웃

문제가 발견되면 직접 수정하고 다시 테스트한다.

테스트하지 않은 기능을 완료했다고 보고하지 않는다.

---

# 27. README 업데이트

README도 현재 구조에 맞게 업데이트한다.

포함:

- 프로젝트 개요
- 행사 명칭
- 관계자 사이트 기능
- 로컬 실행법
- Supabase 설정
- 필요한 DB migration/schema 실행법
- 관리자 계정 생성법
- staff 계정 생성법
- 권한 구조
- Netlify 배포 구조
- 데이터 수정 방법
- 실제 행사 정보 입력 위치
- 문제 발생 시 확인할 설정

README의 행사 명칭도 반드시 다음으로 통일한다.

> 2026년 인천 AI·SW미래채움 교육페스티벌

---

# 28. GitHub 반영 — 반드시 PUSH까지 완료

이번 작업의 최종 완료 기준은 **commit 생성이 아니라 GitHub 원격 저장소에 push가 성공하는 것**이다.

commit까지만 하고 작업을 끝내면 안 된다.

모든 수정과 테스트가 정상적으로 완료되면 다음 순서로 처리한다.

현재 상태 확인:

```bash
git status
git branch --show-current
git remote -v
```

변경사항을 검토한다.

다음과 같은 불필요한 파일이 포함되지 않았는지 확인한다.

- 임시파일
- 테스트 캡처
- 로그
- secret
- service_role key
- 개인 인증 정보
- 로컬 개발용 임시파일

필요한 변경사항을 stage 한다.

```bash
git add -A
```

push에 필요한 commit을 만든다.

```bash
git commit -m "feat: redesign festival site as staff operations portal"
```

그 다음 반드시 현재 branch를 GitHub에 push한다.

```bash
git push origin HEAD
```

최종 완료 조건:

- 로컬 코드 수정 완료
- 기능 테스트 완료
- 오류 수정 완료
- Supabase 확인 완료
- GitHub 원격 저장소에 변경사항 PUSH 완료

**commit까지만 하고 종료하면 작업 미완료다.**

push 성공 여부를 직접 확인하고, GitHub 원격 저장소에 새 commit이 실제 반영된 것을 확인한 뒤에만 완료라고 보고한다.

절대 force push하지 않는다.

절대 다음과 같은 명령으로 사용자의 기존 작업을 날리지 않는다.

- `git reset --hard`
- 강제 checkout으로 사용자 변경 삭제
- 임의의 rebase
- force push

기존 git history를 reset/rebase하여 사용자 작업을 날리지 않는다.

push가 실패하면 정상적인 범위에서 원인을 해결하고 다시 push한다.

다만 인증 또는 권한 문제처럼 현재 환경에서 직접 해결할 수 없는 경우에만:

- 현재 변경사항을 안전하게 보존하고
- 어떤 명령에서
- 어떤 오류가 발생했고
- 사용자가 무엇을 해야 하는지

정확하게 보고한다.

단순 오류라고 바로 포기하지 않는다.

---

# 29. Netlify 배포 확인

현재 GitHub와 Netlify 자동 배포가 연결되어 있다면 GitHub push 이후 자동 배포가 시작되는지 확인한다.

가능한 경우 배포 후 다음 사이트에서도 확인한다.

- https://incheon-aisw-festival.netlify.app/

최소 확인 항목:

- 새 관계자 로그인 화면
- 행사 명칭이 `2026년 인천 AI·SW미래채움 교육페스티벌`로 표시되는지
- CSS 정상 로딩
- JS 정상 로딩
- Supabase 정상 연결
- 로그인 가능
- 인증 없는 내부 정보 노출 없음
- 모바일 레이아웃 정상
- 콘솔 오류 없음

Netlify 자동배포가 연결되어 있지 않다면 임의로 새로운 Netlify 사이트를 만들지 않는다.

현재 연결 상태와 필요한 조치만 정확하게 보고한다.

---

# 30. 최종 보고 형식

모든 작업이 끝난 뒤 다음 형식으로 간결하게 보고한다.

## 변경한 방향

- ...

## 핵심 기능

- ...

## Supabase

- 연결 상태:
- 추가/변경된 테이블:
- Auth 상태:
- RLS 상태:

## 테스트

- 로그인:
- 일정:
- 부스:
- 공지:
- 운영 요청:
- 관리자 CRUD:
- 모바일:
- 콘솔:

## GitHub

- branch:
- commit hash:
- push: 성공 / 실패
- remote:
- GitHub 원격 반영 여부:

## 배포

- Netlify 상태:
- 확인 URL:

## 남은 작업

- 실제 행사 정보가 없어 임시 데이터인 항목
- 사용자가 추후 입력해야 하는 항목

---

# 31. 가장 중요한 원칙

이번 작업은 단순한 화면 리디자인이 아니다.

**「2026년 인천 AI·SW미래채움 교육페스티벌」 관계자가 행사 준비 단계와 행사 당일 스마트폰으로 실제 사용하는 운영 시스템으로 만들어야 한다.**

예쁘게 만드는 것보다 다음 정보를 빠르게 찾고 처리할 수 있는 것이 우선이다.

- 지금 해야 할 일
- 현재 진행 상황
- 다음 일정
- 부스 상태
- 긴급 공지
- 현장 문제 보고
- 담당자 연락
- 필요한 운영 자료
- 행사장 위치

관계자가 필요한 기능을 가능하면 2~3번의 터치 안에 찾을 수 있게 한다.

먼저 현재 코드를 충분히 분석한 뒤:

1. 전체 구조 재설계
2. 실제 기능 구현
3. 브라우저 테스트
4. 오류 수정
5. Supabase 확인
6. GitHub push
7. Netlify 배포 확인

순서로 끝까지 진행한다.

중간에 세부 디자인 선택을 사용자에게 계속 묻지 말고 현재 디자인 시스템과 행사 운영 목적을 기준으로 가장 적절한 방향을 판단하여 구현한다.

---

# 32. 최종 완료 기준

아래 순서를 모두 완료해야 작업 완료다.

```text
기능 구현
→ 브라우저 테스트
→ 오류 수정
→ Supabase 연결 및 실제 저장/조회 확인
→ git add / commit
→ GitHub push
→ GitHub 원격 반영 확인
→ Netlify 자동 배포 확인
→ 배포본 최종 확인
```

`commit`만 하고 종료하지 않는다.

**GitHub push 성공이 이번 작업의 필수 완료 기준이다.**

GitHub push 후 Netlify 자동 배포가 연결되어 있다면 **배포된 사이트 확인까지 완료해야 최종 작업이 끝난다.**
